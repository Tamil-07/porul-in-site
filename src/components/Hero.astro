<section class="hero" aria-labelledby="home-title">
  <div class="wrap">
    <p class="hero__eyebrow">Porul.in / For Tamil Nadu MSMEs</p>
    <h1 id="home-title">Your MSME's<br />extended back office.</h1>
    <p class="hero__dek">The paperwork, profiles and preparation behind your next business opportunity. Bring us the work that keeps coming back to your desk.</p>
    <div class="hero__actions">
      <a class="button button--dark" href="/enquire/">Tell us what's pending <span aria-hidden="true">→</span></a>
      <a class="hero__explore" href="#work">Find your starting point <span aria-hidden="true">↓</span></a>
    </div>
    <p class="hero__notice">Start with one task. We confirm what we can take on, the scope and the fee before work begins.</p>
  </div>
</section>

<style>
  .hero { padding: 56px 0 40px; border-bottom: 1px solid var(--line); background: var(--surface-raised); }
  .hero__eyebrow { margin: 0; color: var(--action-primary); font-size: 13px; font-weight: 700; letter-spacing: 0; }
  h1 { max-width: 22ch; margin: 20px 0; font-family: var(--display); font-size: 64px; font-weight: 600; line-height: 1.06; letter-spacing: 0; }
  .hero__dek { max-width: 56ch; margin: 0; color: var(--text-default); font-size: 20px; line-height: 1.6; }
  .hero__actions { display: flex; flex-wrap: wrap; align-items: center; gap: 12px 24px; margin-top: 24px; }
  .hero__explore { display: inline-flex; align-items: center; min-height: 44px; gap: 10px; font-weight: 700; }
  .hero__explore:hover { text-decoration: underline; text-underline-offset: 4px; }
  .hero__notice { max-width: 70ch; margin: 16px 0 0; color: var(--text-muted); font-size: 14px; }
  @media (max-width: 767px) {
    .hero { padding: 36px 0 28px; }
    h1 { font-size: 44px; }
    .hero__dek { font-size: 18px; }
  }
  @media (max-width: 389px) { h1 { font-size: 36px; } }
</style>
