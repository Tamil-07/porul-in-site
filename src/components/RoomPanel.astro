<section class="room-section section" aria-labelledby="room-heading">
  <div class="wrap">
    <div class="room cham">
      <div class="room__intro">
        <p class="room__kicker">The Room · reviewed entry</p>
        <h2 id="room-heading">A small, reviewed room.</h2>
        <p>A room for people working in Tamil Nadu CNC and precision engineering. It is secondary to Porul.in's published work, and entry stays reviewed.</p>
      </div>
      <div class="room__details">
        <p class="room__label">Two member benefits</p>
        <ul>
          <li>A weekly digest.</li>
          <li>Early access to Porul.in sector intelligence before public publication.</li>
        </ul>
        <a class="room__cta cham" href="/the-room/">Understand the Room <span aria-hidden="true">→</span></a>
      </div>
      <p class="room__boundary"><strong>The boundary:</strong> this is not vendor registration, an RFQ, a sourcing request, or a sales-enquiry channel. Porul.in does not quote, match, broker, source, or fulfil.</p>
    </div>
  </div>
</section>

<style>
  .room {
    --cham: var(--cham-lg);
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(280px, 0.7fr);
    gap: var(--space-8) clamp(var(--space-8), 7vw, var(--space-20));
    padding: clamp(var(--space-8), 6vw, var(--space-16));
    background: var(--surface-inverse);
    color: var(--text-inverse);
  }

  .room__kicker,
  .room__label {
    margin: 0 0 var(--space-3);
    color: #F5A083;
    font-size: var(--type-meta);
    font-weight: 700;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  h2 {
    max-width: 10ch;
    margin: 0 0 var(--space-5);
    font-family: var(--display);
    font-size: var(--type-heading-lg);
    line-height: 1.05;
    text-wrap: balance;
  }

  .room__intro > p:last-child {
    max-width: 54ch;
    margin: 0;
    color: #D0D0D5;
    font-size: var(--type-body-md);
  }

  .room__details {
    padding-top: var(--space-1);
  }

  ul {
    display: grid;
    gap: var(--space-3);
    margin: 0 0 var(--space-6);
    padding: 0;
    list-style: none;
  }

  li {
    padding: 0 0 var(--space-3) var(--space-5);
    border-bottom: 1px solid var(--border-inverse);
    color: #E0E0E4;
    font-size: var(--type-body-sm);
  }

  li::before {
    content: "—";
    float: left;
    margin-left: calc(var(--space-5) * -1);
    color: #F5A083;
  }

  .room__cta {
    --cham: var(--cham-sm);
    display: inline-flex;
    align-items: center;
    min-height: 48px;
    gap: var(--space-2);
    padding: 12px 18px;
    background: var(--action-primary);
    color: var(--action-on-primary);
    font-size: 14px;
    font-weight: 700;
    transition: background var(--motion-standard) var(--ease-standard), transform var(--motion-fast) var(--ease-standard);
  }

  .room__cta:hover {
    background: var(--action-primary-hover);
  }

  .room__cta:active {
    background: var(--action-primary-pressed);
    transform: translateY(1px);
  }

  .room__boundary {
    grid-column: 1 / -1;
    margin: 0;
    padding-top: var(--space-5);
    border-top: 1px solid var(--border-inverse);
    color: #AFAFB7;
    font-size: 13px;
    line-height: 1.7;
  }

  .room__boundary strong {
    color: var(--text-inverse);
  }

  @media (max-width: 767px) {
    .room {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .room__cta {
      width: 100%;
      justify-content: center;
    }
  }

  @media (max-width: 599px) {
    .room {
      padding: var(--space-8) var(--space-5);
    }
  }
</style>
